#!/bin/bash

set -e

case $1 in
    configure)
        /usr/share/buendia/config.d/mysql
        service cron restart
        ;;

    abort-upgrade|abort-remove|abort-deconfigure)
        ;;

    *) exit 1
esac

