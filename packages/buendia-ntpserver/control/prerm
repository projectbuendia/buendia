#!/bin/bash

set -e; . /usr/share/buendia/utils.sh

buendia-divert $1 /etc/ntp.conf

case $1 in
    remove)
        service ntp restart
        ;;

    upgrade|deconfigure)
        ;;

    *) exit 1
esac
