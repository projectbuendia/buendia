#!/bin/bash

set -e
SERVICE=ssh
FILE=/etc/ssh/sshd_config

if [ "$1" = remove ]; then
    [ -L $FILE ] && rm $FILE
    dpkg-divert --remove --rename --divert $FILE.pre-buendia $FILE
    service $SERVICE restart
fi
