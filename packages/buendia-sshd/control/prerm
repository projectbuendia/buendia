#!/bin/bash

set -e; . /usr/share/buendia/utils.sh

buendia-divert $1 /etc/ssh/sshd_config

case $1 in
    remove)
        service ssh restart
        ;;

    upgrade|deconfigure)
        ;;

    *) exit 1
esac
