server {
    listen 9999;
    root /usr/share/buendia;
    server_name _;

    location / {
        fastcgi_pass unix:/var/run/fcgiwrap.socket;
        fastcgi_param SCRIPT_NAME status;
        fastcgi_param SCRIPT_FILENAME /usr/share/buendia/status;
    }
}
