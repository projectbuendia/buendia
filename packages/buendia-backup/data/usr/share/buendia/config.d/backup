#!/bin/bash

set -e; . /usr/share/buendia/utils.sh
SETTINGS=/usr/share/buendia/site/backup

# Generate a default settings file.
if [ ! -e $SETTINGS ]; then
    unindent <<< '
        BACKUP_INTERNAL_DIR=/var/backups/buendia
        BACKUP_INTERNAL_LIMIT_KB=200000
    ' | create $SETTINGS
fi
